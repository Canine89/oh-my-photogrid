<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Always dark ("darkroom"): matches the Compose theme so launch and fold transitions don't flash white. -->
    <style name="Theme.WireframePhoto" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/window_background</item>
    </style>
</resources>
